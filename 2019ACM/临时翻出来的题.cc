#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("RAW/in", "r", stdin);
    freopen("RAW/out", "w", stdout);
    int T;
    int n;
    scanf("%d", &T);
    while(T--) {
        
    }
    return 0;
}