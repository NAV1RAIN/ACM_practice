#include <bits/stdc++.h>
using namespace std;
const int SIZE = 2e5+5;
bool maze[SIZE][SIZE];
int main() {
    freopen("RAW/in", "r", stdin);
    freopen("RAW/out", "w", stdout);
    int T;
    scanf("%d", &T);
    while(T--) {

    }
    return 0;
}