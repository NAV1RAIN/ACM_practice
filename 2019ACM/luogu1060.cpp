#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("in", "r", stdin);
    freopen("out", "w", stdout);
    int n, m;
    scanf("%d%d", &n, &m);
    for(int i = 0; i < m; i++) {

    }
    return 0;
}