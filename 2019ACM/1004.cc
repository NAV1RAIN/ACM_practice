#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("RAW/in", "r", stdin);
    freopen("RAW/out", "w", stdout);
    int T;
    scanf("%d", &T);
    while(T--) {
        init();
        int n, m;
    }
    return 0;
}