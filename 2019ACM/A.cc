#include <bits/stdc++.h>
using namespace std;

int main() {
	int T;
	scanf("%d", &T);
	int t = 1;
	while(T--) {
		int n, k, q;
		scanf("%d%d%d", &n, &k, &q);
		printf("Case #%d: ", t++);
		
	}
	return 0;
}
