#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    scanf("%d", &T);
    long long n, x;
    while(T--) {
        scanf("%lld%lld", &n, &x);
        printf("%lld\n", x*2);
    }
    return 0;
}