#include <bits/stdc++.h>
using namespace std;
const int MOD = 998244353;
int main() {
    int n, m;
    while(~scanf("%d%d", &n, &m)) {
        for(int i = 0; i < m; i++) {
            int a, b;
            scanf("%d%d", &a, &b);
        }
    }
    return 0;
}