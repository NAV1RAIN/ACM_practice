#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, a, b;
    int sx1, sy1, sx2, sy2;
    scanf("%d%d%d", &n, &a, &b);
    scanf("%d%d%d%d", &sx1, &sy1, &sx2, &sy2);
    return 0;
}
