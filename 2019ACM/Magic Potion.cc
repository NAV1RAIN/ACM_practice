#include <cstdio>
#include <cstring>
#include <set>
using namespace std;
const int MAXN = 505;
int mm[MAXN][MAXN];
set<int> ss;
int main() {
    int n, m, k;
    for(int i = 0; i < n; i++) {
        int tt;
        scanf("%d", &tt);
        for(int i = 0; i < tt; i++) {
            
        }
    }
    return 0;
}